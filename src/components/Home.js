import { styled } from "styled-components";

const Home = (props) => {
    return (
            <Container>
                <h1>Hello hiiiiii</h1>
            </Container>
    )
};

const Container = styled.h1`
color:red;

`;

export default Home;